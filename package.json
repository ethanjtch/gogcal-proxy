{
  "name": "gogcal-proxy",
  "version": "1.0.0",
  "description": "Cloudflare-powered Google Calendar sync - CalDAV + OAuth2 gateway for Apple Calendar",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "node scripts/check.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.122.0"
  }
}